# 前端环境变量（Vercel）
# 部署到 Vercel 时，设置为 Render 后端地址
VITE_API_BASE=https://your-render-app.onrender.com/api

# 后端环境变量（Render）
# PORT 由 Render 自动注入，无需手动设置
NODE_ENV=production
JWT_SECRET=zuoyouyingyu-secret-key-2024
